{
  "name": "datadog-prometheus-exporter",
  "version": "0.0.1",
  "description": "prometheus exporter for datadog billable usage metrics",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "author": "nyilmaz",
  "license": "ISC",
  "dependencies": {
    "@datadog/datadog-api-client": "^1.6.0",
    "express": "^4.18.2",
    "prom-client": "^14.1.0"
  }
}
